'use strict';

/**
 * @swagger
 * tags:
 *   name: Health
 *   description: Service health endpoints
 */

/**
 * @swagger
 * /api/health:
 *   get:
 *     summary: Service health check
 *     description: Returns OK status with database connection state.
 *     tags: [Health]
 *     responses:
 *       200:
 *         description: Health status.
 */
module.exports = {};
