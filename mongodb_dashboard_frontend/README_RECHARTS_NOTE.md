If you encounter a build error about missing 'recharts', install it:

npm install recharts

The Users Analytics page relies on Recharts components: LineChart, BarChart, PieChart, ResponsiveContainer, CartesianGrid, Tooltip, Legend.
