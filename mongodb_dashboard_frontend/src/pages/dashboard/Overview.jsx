import React from 'react';
import OverviewWrapper from './OverviewWrapper.jsx';

/**
 * PUBLIC_INTERFACE
 * Overview page wrapper
 * Renders the full OverviewWrapper which includes KPI count cards and keeps Users Created chart untouched elsewhere.
 */
export default function Overview() {
  return <OverviewWrapper />;
}
